#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @n: the int to check
* Return: the absolute value of int
*/
if (n > 0)
	{
		return (n);
		n = n;
	}
	else if (n == 0)
	{
		return (n);
		n = n;
	}
	else if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	return (n);
}
 
